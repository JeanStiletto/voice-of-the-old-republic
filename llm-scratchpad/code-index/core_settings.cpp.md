# core_settings.cpp (16 lines)

Implements the `acc::core::Get()` stub. Returns a static default-constructed NavSettings. Phase 7 will replace this with config-file-backed state; signature stays stable.

## Declarations (in source order)

- L5 — `namespace acc::core`
- L5 — `const NavSettings& Get()`
