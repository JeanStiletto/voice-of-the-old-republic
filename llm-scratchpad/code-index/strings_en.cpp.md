# strings_en.cpp (~300 lines)

English string table. Plain ASCII — no escape sequences needed. String literals not indexed per task rules.

## Declarations (in source order)

- L7 — `namespace acc::strings::lang_en`
- L9 — `const char* Get(Id id)`
  note: switch over all Id values; returns English string literal for each
