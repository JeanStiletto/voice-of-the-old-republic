**What this changes:**

**Related issue:** (optional — #123)

**Tested in-game:** (screen reader + OS, and which area / screen / scenario you verified; skip only if this is a doc or build-only change)

**Reverse-engineering note:** (if this touches an engine offset, hook address, or vtable index, cite the source you verified it against — a query result, a session log, a decompile note. Addresses, offsets, and signatures are fine to include; do NOT paste a verbatim decompiled function body or attach the Ghidra project / game binary.)
