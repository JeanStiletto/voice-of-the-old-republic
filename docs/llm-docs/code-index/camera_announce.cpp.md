# camera_announce.cpp (347 lines)

Per-tick camera-direction announcer: derives compass yaw from atan2(player - camera) each tick (the orbital camera always looks at the character), buckets into 8×45° sectors with 5° hysteresis, and speaks sector changes via `prism::SpeakUrgent` (routes around NVDA's typed-char cancel since the user is often holding A/D). Detects active rotation from the camera's own angular velocity (not specific key reads, so it's key-binding-independent) and then speaks for exactly one of three reasons, resolved in a single decision block: `stopped` (rotation just ended — immediate, so it doesn't read as lag), `turning` (still rotating — rate-limited running readout, without which a held turn is silent until release), `settled` (sector held for kQuietMs). Mutes while an engine cinematic drives the camera (`HasActiveDialogPanel`) or while `camera_orient` is auto-driving, and silently re-anchors after either ends so the scripted/auto direction is never spoken as a player turn. Also exposes `AnnounceCurrentFacing` for one-shot readouts (e.g. after a door autoturn) with a dedup window against the per-tick announcer's own recent announce. Talks to camera_orient, engine_compass, engine_panels, engine_player, hotkeys, prism.

**Discontinuity guard (added 2026-08-13).** The heading mixes two sources that don't always describe the same moment: a Tab leader swap switches the player half to the new leader instantly while the camera half lags, so the compass flips ~180° for a frame or two and snaps back. Such frames are classified as discontinuities — by leader identity change (`GetClientLeader`) or by a rate above `kMaxTurnDps` — which forces `rotating`/`stopped` false and holds all speech for `kDiscontinuitySettleMs`. Each further discontinuity re-arms the window, so a Tab burst collapses to one cue for the leader the player stopped on, and a flip that ends where it began says nothing. Before this, a one-frame 11570°/s jump satisfied the lower-bound-only rotation test and spoke a direction the camera never faced (found in beta log `danks932logs`, present in dev logs too).

## Declarations (in source order)

- L23-67 — sector/timing constants (`kSectorSize`=45, `kHalfSector`=22.5, `kHysteresis`=5, `kQuietMs`=250, `kMinIntervalHeldMs`=300, `kRotatingThresholdDps`=30, `kMaxTurnDps`=1200, `kDiscontinuitySettleMs`=500, `kMinXYDistance`=0.1)
- L69 — `float AngularDelta(float a, float b)` — signed shortest delta in (-180,180]
- L73-93 — module statics: last/pending spoken sector, change/spoken timestamps, cached compass, velocity-tracking prev compass+timestamp, `s_prevRotating`, `s_lastLeader`, `s_discontinuityAt`, `s_mutedByCutscene`
- L95 — `bool ReadCameraCompass(float& outCompass)` — atan2(player-camera) → EngineYawToCompass; false if XY distance < kMinXYDistance
- L115 — `void Tick()` — main state machine: reset on invalid read, mute+re-anchor around HasActiveDialogPanel and camera_orient::IsActive(), first-tick anchor, angular-velocity-derived rotation detection bounded at both ends, discontinuity classification + settle window, sticky-hysteresis sector tracking, single stopped/turning/settled speech decision
- L296 — `bool AnnounceCurrentFacing(unsigned int dedupMs)` — one-shot readout with dedup against s_lastSpokenSector/s_lastSpokenAt; false during dialog panel or unresolved camera
- L338 — `bool TryGetCameraEngineYawDegrees(float& out)` — exposes s_lastCamCompass converted back to engine frame; false until Tick has anchored
